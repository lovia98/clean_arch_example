package com.example.clean_architecture.bunckpal.account.domain;

public class ActivityWindow {
    public Account.AccountId calculateBalance(Account.AccountId id) {
        return id;
    }

    public void addActivity(Activity withdrawal) {

    }
}
